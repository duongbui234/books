## Topic 31. Inheritance Tax — Topic 31. Thuế Kế Thừa

Do you program in an objectoriented language? Do you use **inheritance**?

> Bạn có lập trình bằng ngôn ngữ hướng đối tượng không? Bạn có dùng kế thừa (inheritance) không?

If so, stop! It probably isn’t what you want to do.

> Nếu có, hãy dừng lại! Rất có thể đó không phải điều bạn thực sự muốn làm.

Let’s see why.

> Hãy cùng xem tại sao.

### Some Background — Đôi Nét Bối Cảnh

Inheritance first appeared in Simula 67 in 1969. It was an elegant solution to the problem of queuing multiple types of events on the same list. The Simula approach was to use something called prefix classes . You could write something like this:

> Kế thừa xuất hiện lần đầu trong Simula 67 vào năm 1969. Đó là một giải pháp tao nhã cho bài toán xếp hàng nhiều loại sự kiện khác nhau trên cùng một danh sách. Cách tiếp cận của Simula là dùng thứ gọi là lớp tiền tố (prefix classes). Bạn có thể viết kiểu như thế này:

link CLASS car; ... implementation of car

> link CLASS car; ... cài đặt của car

link CLASS bicycle; ... implementation of bicycle

> link CLASS bicycle; ... cài đặt của bicycle

The link is a prefix class that adds the functionality of linked lists. This lets you add both cars and bicycles to the list of things waiting at (say) a traffic light. In current terminology, link would be a parent class.

> link là một lớp tiền tố bổ sung chức năng của danh sách liên kết. Nhờ đó bạn có thể thêm cả car lẫn bicycle vào danh sách những thứ đang chờ ở (chẳng hạn) một đèn giao thông. Theo thuật ngữ ngày nay, link sẽ là một lớp cha.

The mental model used by Simula programmers was that the instance data and implementation of class link was prepended to the implementation of classes car and bicycle . The link part was almost viewed as being a container that carried around cars and bicycles. This gave them a form of polymorphism: cars and bicycles both implemented the link interface because they both contained the link code.

> Mô hình tư duy mà các lập trình viên Simula dùng là: dữ liệu thể hiện và phần cài đặt của lớp link được gắn vào phía trước phần cài đặt của các lớp car và bicycle. Phần link gần như được xem là một cái thùng chứa mang theo các car và bicycle. Điều này cho họ một dạng đa hình: cả car lẫn bicycle đều hiện thực giao diện link, vì cả hai đều chứa mã của link.

After Simula came Smalltalk. Alan Kay, one of the creators of Smalltalk, describes in a 2019 Quora answer [44] why Smalltalk has inheritance:

> Sau Simula là Smalltalk. Alan Kay, một trong những người tạo ra Smalltalk, đã mô tả trong một câu trả lời trên Quora năm 2019 [44] vì sao Smalltalk có kế thừa:

So when I designed Smalltalk-72—and it was a lark for fun while thinking about Smalltalk-71—I thought it would be fun to use its Lisp-like dynamics to do experiments with “differential programming” (meaning: various ways to accomplish “this is like that except”).

> Vậy nên khi tôi thiết kế Smalltalk-72 — và đó chỉ là một trò vui trong lúc nghĩ về Smalltalk-71 — tôi nghĩ sẽ thú vị nếu dùng tính động kiểu Lisp của nó để làm thí nghiệm với "lập trình theo sai biệt" (differential programming) (nghĩa là: đủ kiểu cách để diễn đạt "cái này giống cái kia, ngoại trừ…").

This is subclassing purely for behavior.

> Đây thuần túy là chuyện kế thừa lớp con (subclassing) vì hành vi.

These two styles of inheritance (which actually had a fair amount in common) developed over the following decades. The Simula approach, which suggested inheritance was a way of combining types, continued in languages such as C++ and Java. The Smalltalk school, where inheritance was a dynamic organization of behaviors, was seen in languages such as Ruby and JavaScript.

> Hai phong cách kế thừa này (thực ra có khá nhiều điểm chung) phát triển qua mấy thập kỷ tiếp theo. Cách tiếp cận của Simula, vốn ngụ ý rằng kế thừa là một cách kết hợp các kiểu (type), tiếp tục trong những ngôn ngữ như C++ và Java. Trường phái Smalltalk, nơi kế thừa là một cách tổ chức hành vi một cách động, xuất hiện trong những ngôn ngữ như Ruby và JavaScript.

So, now we’re faced with a generation of OO developers who use inheritance for one of two reasons: they don’t like typing, or they like types.

> Thế là giờ đây ta đối mặt với cả một thế hệ lập trình viên hướng đối tượng dùng kế thừa vì một trong hai lý do: hoặc họ lười gõ, hoặc họ thích kiểu (type).

Those who don’t like typing save their fingers by using

> Những người lười gõ tiết kiệm mấy ngón tay của mình bằng cách

inheritance to add common functionality from a base class into child classes: class User and class Product are both subclasses of ActiveRecord::Base .

> dùng kế thừa để đưa chức năng chung từ một lớp cơ sở vào các lớp con: class User và class Product đều là lớp con của ActiveRecord::Base.

Those who like types use inheritance to express the relationship between classes: a Car is-a-kind-of Vehicle .

> Còn những người thích kiểu thì dùng kế thừa để diễn đạt quan hệ giữa các lớp: một Car là-một-loại-của Vehicle.

Unfortunately both kinds of inheritance have problems.

> Đáng tiếc, cả hai kiểu kế thừa đều có vấn đề.

### Problems Using Inheritance To Share Code — Những Vấn Đề Khi Dùng Kế Thừa Để Dùng Chung Mã

Inheritance is **coupling**. Not only is the child class coupled to the parent, the parent’s parent, and so on, but the code that uses the child is also coupled to all the ancestors. Here’s an example:

> Kế thừa là sự ghép nối (coupling). Không chỉ lớp con bị ghép nối với lớp cha, rồi cha của cha, và cứ thế, mà cả đoạn mã sử dụng lớp con cũng bị ghép nối với tất cả các tổ tiên của nó. Đây là một ví dụ:

class Vehicle def initialize @speed = 0 end def stop @speed = 0 end def move_at(speed) @speed = speed end end

> class Vehicle def initialize @speed = 0 end def stop @speed = 0 end def move_at(speed) @speed = speed end end

class Car < Vehicle def info "I'm car driving at #{ @speed }" end end

> class Car < Vehicle def info "I'm car driving at #{ @speed }" end end

# top-level code my_ride = Car.new my_ride.move_at(30) — mã ở tầng top-level my_ride = Car.new my_ride.move_at(30)

When the top-level calls my_car.move_at , the method being invoked is in Vehicle , the parent of Car .

> Khi mã top-level gọi my_car.move_at, phương thức thực sự được gọi lại nằm trong Vehicle, lớp cha của Car.

Now the developer in charge of Vehicle changes the API, so move_at becomes set_velocity , and the instance variable @speed becomes @velocity .

> Giờ lập trình viên phụ trách Vehicle thay đổi API: move_at trở thành set_velocity, còn biến thể hiện @speed trở thành @velocity.

An API change is expected to break clients of Vehicle class. But the top-level is not: as far as it is concerned it is using a Car . What the Car class does in terms of implementation is not the concern of the top-level code, but it still breaks.

> Một thay đổi API thì đương nhiên được dự liệu là sẽ làm hỏng những nơi dùng lớp Vehicle. Nhưng mã top-level đâu nằm trong số đó: theo cách nó hiểu thì nó đang dùng một Car. Lớp Car cài đặt ra sao không phải là chuyện mã top-level phải bận tâm, vậy mà nó vẫn hỏng.

Similarly the name of an instance variable is purely an **internal** implementation detail, but when Vehicle changes it also (silently) breaks Car .

> Tương tự, tên của một biến thể hiện thuần túy là chi tiết cài đặt nội bộ, vậy mà khi Vehicle đổi nó thì Car cũng hỏng theo (và hỏng một cách âm thầm).

So much coupling.

> Ghép nối ơi là ghép nối.

Problems Using Inheritance to Build Types Some folks view inheritance as a way of defining new types. Their favorite design diagram shows class hierarchies. They view problems the way Victorian gentleman scientists viewed nature, as something to be broken down into categories.

> Những Vấn Đề Khi Dùng Kế Thừa Để Dựng Kiểu Một số người xem kế thừa như một cách định nghĩa kiểu mới. Sơ đồ thiết kế ưa thích của họ là những cây phân cấp lớp. Họ nhìn các bài toán giống như mấy quý ông khoa học gia thời Victoria nhìn tự nhiên: như một thứ cần được chia nhỏ thành từng phạm trù.

Unfortunately, these diagrams soon grow into wall-covering monstrosities, layer-upon-layer added in order to express the smallest nuance of differentiation between classes. This added complexity can make the application more brittle, as changes can ripple up and down many layers.

> Đáng tiếc, mấy sơ đồ này chẳng mấy chốc phình to thành những con quái vật phủ kín tường, lớp chồng lên lớp được thêm vào chỉ để diễn đạt khác biệt bé tí xíu giữa các lớp. Độ phức tạp tăng thêm này có thể khiến ứng dụng dễ vỡ hơn, vì một thay đổi có thể lan truyền lên xuống qua nhiều tầng.

Even worse, though, is the multiple inheritance issue. A Car may be a kind of Vehicle , but it can also be a kind of Asset , InsuredItem , LoanCollateral and so on. Modeling this correctly would need

> Nhưng tệ hơn nữa là vấn đề đa kế thừa. Một Car có thể là một loại Vehicle, nhưng nó cũng có thể là một loại Asset, InsuredItem, LoanCollateral, v.v. Mô hình hóa chuyện này cho đúng thì cần đến

multiple inheritance.

> đa kế thừa.

C++ gave multiple inheritance a bad name in the 1990s because of some questionable disambiguation semantics. As a result, many current OO languages don’t offer it. So, even if you’re happy with complex type trees, you won’t be able to model your domain accurately anyway.

> C++ làm cho đa kế thừa mang tiếng xấu hồi thập niên 1990 vì một vài ngữ nghĩa khử nhập nhằng đáng ngờ. Hệ quả là nhiều ngôn ngữ hướng đối tượng hiện nay không hề cung cấp nó. Vậy nên, kể cả khi bạn thấy hài lòng với những cây kiểu phức tạp, bạn vẫn sẽ không thể mô hình hóa miền nghiệp vụ của mình một cách chính xác.

**Tip 51: Don’t Pay Inheritance Tax**

> **Tip 51: Đừng Đóng Thuế Kế Thừa**

### The Alternatives Are Better — Các Lựa Chọn Thay Thế Tốt Hơn

Let us suggest three techniques that mean you should never need to use inheritance again:

> Hãy để chúng tôi gợi ý ba kỹ thuật giúp bạn không bao giờ còn cần dùng kế thừa nữa:

Interfaces and protocols

> Giao diện và giao thức (interfaces and protocols)

Delegation

> Ủy quyền (delegation)

Mixins and traits

> Mixin và trait

Interfaces and Protocols

> Giao Diện và Giao Thức

Most OO languages allow you to specify that a class implements one or more sets of behaviors. You could say, for example, that a Car class implements the Drivable behavior and the Locatable behavior. The syntax used for doing this varies: in Java, it might look like this:

> Hầu hết các ngôn ngữ hướng đối tượng cho phép bạn khai báo rằng một lớp hiện thực một hay nhiều tập hành vi. Chẳng hạn, bạn có thể nói rằng lớp Car hiện thực hành vi Drivable và hành vi Locatable. Cú pháp để làm việc này thì mỗi ngôn ngữ mỗi khác; trong Java, nó có thể trông như thế này:

public class Car implements Drivable, Locatable {

> public class Car implements Drivable, Locatable {

// Code for class Car. This code must include // the functionality of both Drivable // and Locatable

> // Mã cho lớp Car. Mã này phải bao gồm // chức năng của cả Drivable // lẫn Locatable

}

> }

Drivable and Locatable are what Java calls interfaces ; other languages call them protocols , and some call them traits (although this is not what we’ll be calling a trait later).

> Drivable và Locatable chính là cái mà Java gọi là interface (giao diện); các ngôn ngữ khác gọi chúng là protocol (giao thức), và một số gọi là trait (dù đây không phải thứ mà lát nữa chúng tôi sẽ gọi là trait).

Interfaces are defined like this:

> Giao diện được định nghĩa như thế này:

public interface Drivable { double getSpeed(); void stop(); }

> public interface Drivable { double getSpeed(); void stop(); }

public interface Locatable() { Coordinate getLocation(); boolean locationIsValid(); }

> public interface Locatable() { Coordinate getLocation(); boolean locationIsValid(); }

These declarations create no code: they simply say that any class that implements Drivable must implement the two methods getSpeed and stop , and a class that’s Locatable must implement getLocation and locationIsValid . This means that our previous class definition of Car will only be valid if it includes all four of these methods.

> Những khai báo này chẳng sinh ra mã nào cả: chúng chỉ đơn giản nói rằng bất kỳ lớp nào hiện thực Drivable đều phải cài đặt hai phương thức getSpeed và stop, còn một lớp là Locatable thì phải cài đặt getLocation và locationIsValid. Nghĩa là định nghĩa lớp Car ở trên chỉ hợp lệ nếu nó có đủ cả bốn phương thức này.

What makes interfaces and protocols so powerful is that we can use them as types, and any class that implements the appropriate interface will be compatible with that type. If Car and Phone both implement Locatable , we could store both in a list of locatable items:

> Điều làm cho giao diện và giao thức mạnh mẽ đến vậy là ta có thể dùng chúng như những kiểu, và bất kỳ lớp nào hiện thực đúng giao diện tương ứng sẽ tương thích với kiểu ấy. Nếu cả Car lẫn Phone đều hiện thực Locatable, ta có thể cất cả hai vào cùng một danh sách các phần tử locatable:

List<Locatable> items = new ArrayList<>();

> List<Locatable> items = new ArrayList<>();

items.add( new Car(...)); items.add( new Phone(...)); items.add( new Car(...)); // ...

> items.add( new Car(...)); items.add( new Phone(...)); items.add( new Car(...)); // ...

We can then **process** that list, safe in the knowledge that every item has getLocation and locationIsValid :

> Sau đó ta có thể xử lý danh sách ấy, yên tâm rằng mọi phần tử đều có getLocation và locationIsValid:

void printLocation(Locatable item) { if (item.locationIsValid() { print(item.getLocation().asString()); }

> void printLocation(Locatable item) { if (item.locationIsValid() { print(item.getLocation().asString()); }

// ...

> // ...

items.forEach(printLocation);

> items.forEach(printLocation);

**Tip 52: Prefer Interfaces to Express Polymorphism**

> **Tip 52: Ưu Tiên Giao Diện Để Diễn Đạt Tính Đa Hình**

Interfaces and protocols give us polymorphism without inheritance.

> Giao diện và giao thức cho ta tính đa hình mà không cần kế thừa.

Delegation Inheritance encourages developers to create classes whose objects have large numbers of methods. If a parent class has 20 methods, and the subclass wants to make use of just two of them, its objects will still have the other 18 just lying around and callable. The class has lost control of its interface. This is a

> Ủy Quyền Kế thừa khuyến khích lập trình viên tạo ra những lớp mà đối tượng của chúng có hằng hà sa số phương thức. Nếu một lớp cha có 20 phương thức, và lớp con chỉ muốn dùng đúng hai trong số đó, thì các đối tượng của nó vẫn cứ kéo theo 18 phương thức kia, nằm chình ình và gọi được. Lớp đã đánh mất quyền kiểm soát giao diện của chính nó. Đây là một

common problem—many persistence and UI frameworks insist that application components subclass some supplied base class:

> vấn đề phổ biến — nhiều framework lưu trữ và giao diện người dùng cứ khăng khăng bắt các thành phần ứng dụng phải kế thừa từ một lớp cơ sở nào đó mà chúng cung cấp:

class Account < PersistenceBaseClass end

> class Account < PersistenceBaseClass end

The Account class now carries all of the persistence class’s API around with it. Instead, imagine an alternative using delegation, as in the following example:

> Giờ thì lớp Account phải vác theo toàn bộ API của lớp lưu trữ đi khắp nơi. Thay vào đó, hãy hình dung một cách khác dùng ủy quyền, như ví dụ sau:

class Account def initialize(. . .) @repo = Persister.for(self) end

> class Account def initialize(. . .) @repo = Persister.for(self) end

def save @repo.save() end end

> def save @repo.save() end end

We now expose none of the framework API to the clients of our Account class: that **decoupling** is now broken. But there’s more. Now that we’re no longer constrained by the API of the framework we’re using, we’re free to create the API we need. Yes, we could do that before, but we always ran the risk that the interface we wrote can be bypassed, and the persistence API used instead. Now we control everything.

> Giờ ta chẳng phơi bày chút nào API của framework ra cho những nơi dùng lớp Account của mình: mối ghép nối đó nay đã bị cắt đứt. Nhưng chưa hết. Vì không còn bị trói buộc bởi API của framework đang dùng, ta được tự do tạo ra đúng cái API mà mình cần. Đúng là trước kia ta cũng làm được vậy, nhưng luôn có nguy cơ giao diện ta viết ra bị đi vòng qua, và người ta dùng thẳng API lưu trữ. Còn giờ thì ta kiểm soát mọi thứ.

**Tip 53: Delegate to Services: Has-A Trumps Is-A**

> **Tip 53: Hãy Ủy Quyền Cho Dịch Vụ: Has-A Thắng Is-A**

In fact, we can take this a step further. Why should an Account have to know how to persist itself? Isn’t its job to know and enforce the account business rules?

> Thật ra, ta còn có thể đẩy chuyện này đi xa thêm một bước. Tại sao một `Account` lại phải biết cách tự lưu trữ chính nó? Chẳng phải việc của nó là biết và thực thi các quy tắc nghiệp vụ của tài khoản hay sao?

class Account # nothing but account stuff

> class Account # không gì ngoài chuyện tài khoản

end

> end

class AccountRecord # wraps an account with the ability # to be fetched and stored end

> class AccountRecord # bọc một account với khả năng # được lấy về và lưu trữ end

Now we’re really decoupled, but it has come at a cost. We’re having to write more code, and typically some of it will be boilerplate: it’s likely that all our record classes will need a find method, for example.

> Giờ thì ta thực sự đã gỡ ghép nối, nhưng cái giá phải trả là có. Ta phải viết nhiều mã hơn, và thường thì một phần trong đó sẽ là mã khuôn mẫu (boilerplate): chẳng hạn, nhiều khả năng mọi lớp record của ta đều cần một phương thức `find`.

Fortunately, that’s what mixins and traits do for us.

> May thay, đó chính là việc mà mixin và trait làm cho ta.

Mixins, Traits, Categories, Protocol Extensions, … As an industry, we love to give things names. Quite often we’ll give the same thing many names. More is better, right?

> Mixin, Trait, Category, Protocol Extension, … Trong ngành này, ta mê đặt tên cho mọi thứ. Khá thường xuyên ta đặt nhiều cái tên cho cùng một thứ. Nhiều thì tốt hơn, đúng không?

That’s what we’re dealing with when we look at mixins. The basic idea is simple: we want to be able to extend classes and objects with new functionality without using inheritance. So we create a set of these functions, give that set a name, and then somehow extend a class or object with them. At that point, you’ve created a new class or object that combines the capabilities of the original and all its mixins. In most cases, you’ll be able to make this extension even if you don’t have access to the source code of the class you’re extending.

> Đó chính là tình cảnh ta gặp phải khi nhìn vào mixin. Ý tưởng cơ bản thì đơn giản: ta muốn có thể mở rộng các lớp và đối tượng bằng chức năng mới mà không cần dùng kế thừa. Vậy nên ta tạo ra một tập hàm, đặt cho tập đó một cái tên, rồi bằng cách nào đó mở rộng một lớp hay đối tượng bằng chúng. Tới lúc ấy, bạn đã tạo ra một lớp hay đối tượng mới kết hợp năng lực của bản gốc và tất cả các mixin của nó. Trong hầu hết trường hợp, bạn sẽ làm được phép mở rộng này ngay cả khi không có quyền truy cập mã nguồn của lớp mà bạn đang mở rộng.

Now the implementation and name of this feature varies between languages. We’ll tend to call them mixins here, but we really want you to think of this as a language-agnostic feature. The important thing is the capability that all these implementations have: merging functionality between existing things and new things .

> Giờ thì cách cài đặt và tên gọi của tính năng này khác nhau tùy ngôn ngữ. Ở đây ta sẽ thiên về gọi chúng là mixin, nhưng ta thực sự muốn bạn nghĩ về nó như một tính năng không phụ thuộc ngôn ngữ. Điều quan trọng là năng lực mà tất cả các cách cài đặt này đều có: hợp nhất chức năng giữa thứ đã có sẵn và thứ mới.

As an example, let’s go back to our AccountRecord example. As we left it, an AccountRecord needed to know about both accounts and about our persistence framework. It also needed to delegate all the methods in the persistence layer that it wanted to expose to the outside world.

> Lấy ví dụ, hãy quay lại ví dụ AccountRecord của chúng ta. Ở chỗ ta bỏ dở, một AccountRecord cần biết cả về account lẫn về framework lưu trữ của ta. Nó còn phải ủy quyền lại tất cả những phương thức của tầng lưu trữ mà nó muốn phơi ra cho thế giới bên ngoài.

Mixins give us an alternative. First, we could write a mixin that implements (for example) two of three of the standard finder methods. We could then add them into AccountRecord as a mixin. And, as we write new classes for persisted things, we can add the mixin to them, too:

> Mixin cho ta một lựa chọn khác. Trước hết, ta có thể viết một mixin hiện thực (chẳng hạn) hai trong ba phương thức tìm kiếm tiêu chuẩn. Rồi ta thêm chúng vào AccountRecord dưới dạng một mixin. Và khi viết những lớp mới cho các thứ cần lưu trữ, ta cũng có thể thêm mixin ấy vào chúng luôn:

mixin CommonFinders { def find(id) { ... } def findAll() { ... } end

> mixin CommonFinders { def find(id) { ... } def findAll() { ... } end

class AccountRecord extends BasicRecord with CommonFinders class OrderRecord extends BasicRecord with CommonFinders

> class AccountRecord extends BasicRecord with CommonFinders class OrderRecord extends BasicRecord with CommonFinders

We can take this a lot further. For example, we all know our business objects need validation code to prevent bad data from infiltrating our calculations. But exactly what do we mean by validation ?

> Ta có thể đẩy chuyện này đi xa hơn nhiều. Ví dụ, ai cũng biết các đối tượng nghiệp vụ của mình cần mã kiểm tra hợp lệ (validation) để ngăn dữ liệu xấu len lỏi vào các phép tính. Nhưng chính xác thì ta hiểu validation là gì?

If we take an account, for example, there are probably many different layers of validation that could be applied:

> Lấy ví dụ một tài khoản, có lẽ có nhiều tầng kiểm tra hợp lệ khác nhau có thể áp dụng:

Validating that a hashed password matches one entered by the user

> Kiểm tra rằng mật khẩu đã băm khớp với mật khẩu người dùng nhập vào

Validating form data entered by the user when an account is created

> Kiểm tra dữ liệu biểu mẫu mà người dùng nhập khi một tài khoản được tạo

Validating form data entered by an admin person updating the user details

> Kiểm tra dữ liệu biểu mẫu mà một quản trị viên nhập khi cập nhật thông tin người dùng

Validating data added to the account by other system components

> Kiểm tra dữ liệu được các thành phần khác của hệ thống thêm vào tài khoản

Validating data for consistency before it is persisted

> Kiểm tra tính nhất quán của dữ liệu trước khi nó được lưu trữ

A common (and we believe less-than-ideal) approach is to bundle all the validations into a single class (the business object/persistence object) and then add flags to control which fire in which circumstances.

> Một cách làm phổ biến (mà chúng tôi cho là chưa lý tưởng) là gom tất cả các kiểm tra hợp lệ vào một lớp duy nhất (đối tượng nghiệp vụ kiêm đối tượng lưu trữ) rồi thêm các cờ để điều khiển cái nào kích hoạt trong hoàn cảnh nào.

We think a better way is to use mixins to create specialized classes for appropriate situations:

> Chúng tôi nghĩ cách hay hơn là dùng mixin để tạo ra các lớp chuyên biệt cho từng tình huống phù hợp:

class AccountForCustomer extends Account with AccountValidations,AccountCustomerValidations

> class AccountForCustomer extends Account with AccountValidations,AccountCustomerValidations

class AccountForAdmin extends Account with AccountValidations,AccountAdminValidations

> class AccountForAdmin extends Account with AccountValidations,AccountAdminValidations

Here, both derived classes include validations common to all account objects. The **customer** variant also includes validations appropriate for the customer-facing APIs, while the admin variant contained (the presumably less restrictive) admin validations.

> Ở đây, cả hai lớp dẫn xuất đều bao gồm các kiểm tra hợp lệ chung cho mọi đối tượng tài khoản. Biến thể dành cho khách hàng còn bao gồm các kiểm tra phù hợp cho các API hướng tới khách hàng, trong khi biến thể dành cho admin chứa các kiểm tra dành cho admin (mà có lẽ ít ràng buộc hơn).

Now, by passing instances of AccountForCustomer or AccountForAdmin back and forth, our code automatically ensures the correct validation is applied.

> Giờ đây, chỉ cần truyền qua lại các thể hiện của AccountForCustomer hay AccountForAdmin, mã của ta tự động bảo đảm đúng bộ kiểm tra hợp lệ được áp dụng.

**Tip 54: Use Mixins to Share Functionality**

> **Tip 54: Dùng Mixin Để Dùng Chung Chức Năng**

### Inheritance Is Rarely The Answer — Kế Thừa Hiếm Khi Là Câu Trả Lời

We’ve had a quick look at three alternatives to traditional class inheritance:

> Ta đã lướt qua nhanh ba lựa chọn thay thế cho kế thừa lớp truyền thống:

Interfaces and protocols

> Giao diện và giao thức

Delegation

> Ủy quyền

Mixins and traits

> Mixin và trait

Each of these methods may be better for you in different circumstances, depending on whether your goal is sharing type information, adding functionality, or sharing methods. As with anything in programming, aim to use the technique that best expresses your intent.

> Mỗi phương pháp trong số này có thể tốt hơn cho bạn trong những hoàn cảnh khác nhau, tùy vào việc mục tiêu của bạn là dùng chung thông tin kiểu, bổ sung chức năng, hay dùng chung phương thức. Như với mọi thứ trong lập trình, hãy hướng tới việc dùng kỹ thuật diễn đạt ý định của bạn rõ ràng nhất.

And try not to drag the whole jungle along for the ride.

> Và cố đừng kéo lê cả khu rừng theo cho chuyến đi.

### Related Sections Include — Các mục liên quan

**Topic** 8, The Essence of Good Design

> Topic 8, Tinh Túy Của Thiết Kế Tốt

Topic 10, **Orthogonality**

> Topic 10, Tính Trực Giao

Topic 28, Decoupling

> Topic 28, Gỡ Ghép Nối

### Challenges — Thử thách

The next time you find yourself subclassing, take a minute to examine the options. Can you achieve what you want with interfaces, delegation, and/or mixins? Can you reduce coupling by doing so?

> Lần tới khi bạn thấy mình đang viết lớp con, hãy dành một phút xem xét các lựa chọn. Bạn có thể đạt được điều mình muốn bằng giao diện, ủy quyền, và/hoặc mixin không? Làm vậy bạn có giảm được sự ghép nối không?

> Let all your things have their places; let each part of your business have its time. Benjamin Franklin, Thirteen Virtues, autobiography

> Hãy để mọi thứ của bạn có chỗ của nó; hãy để mỗi phần việc của bạn có thời gian của nó. Benjamin Franklin, Mười Ba Đức Hạnh, tự truyện
