## PART III Batch Computational Patterns — PART III Các mẫu tính toán theo lô

The preceding chapter described patterns for reliable, long-running server applications. This section describes patterns for **batch processing**. In contrast to long-running applications, batch processes are expected to only run for a short period of time. Examples of a batch process include generating aggregation of user telemetry data, analyzing sales data for daily or weekly reporting, or transcoding video files. Batch processes are generally characterized by the need to process large amounts of data quickly using parallelism to speed up the processing. The most famous pattern for distributed batch processing is the **MapReduce** pattern, which has become an entire industry in itself. However, there are several other patterns that are useful for batch processing, which are described in the following chapters.

> Chương trước đã mô tả các mẫu dành cho ứng dụng máy chủ chạy dài hạn, tin cậy. Phần này mô tả các mẫu cho xử lý theo lô (batch). Khác với ứng dụng chạy dài hạn, tiến trình theo lô được kỳ vọng chỉ chạy trong một khoảng thời gian ngắn. Ví dụ về một tiến trình theo lô bao gồm tổng hợp dữ liệu telemetry của người dùng, phân tích dữ liệu bán hàng cho báo cáo ngày hoặc tuần, hoặc chuyển mã (transcode) các tệp video. Tiến trình theo lô nhìn chung được đặc trưng bởi nhu cầu xử lý lượng lớn dữ liệu một cách nhanh chóng bằng cách dùng tính song song để tăng tốc độ xử lý. Mẫu nổi tiếng nhất cho xử lý theo lô phân tán là mẫu MapReduce, vốn đã trở thành một ngành công nghiệp riêng. Tuy nhiên, còn nhiều mẫu khác hữu ích cho xử lý theo lô, sẽ được mô tả trong các chương sau.

# CHAPTER 10 Work Queue Systems — CHAPTER 10 Hệ thống hàng đợi công việc

The simplest form of batch processing is a **work queue** . In a work queue system, there is a batch of work to be performed. Each piece of work is wholly independent of the other and can be processed without any interactions. Generally, the goals of the work queue system are to ensure that each piece of work is processed within a certain amount of time. Workers are scaled up or scaled down to ensure that the work can be handled. An illustration of a generic work queue is shown in Figure 10-1 .

> Dạng đơn giản nhất của xử lý theo lô là hàng đợi công việc (work queue). Trong một hệ thống hàng đợi công việc, có một lô công việc cần thực hiện. Mỗi đơn vị công việc hoàn toàn độc lập với các đơn vị khác và có thể được xử lý mà không cần bất kỳ tương tác nào. Nhìn chung, mục tiêu của hệ thống hàng đợi công việc là đảm bảo rằng mỗi đơn vị công việc được xử lý trong một khoảng thời gian nhất định. Các worker được mở rộng (scale up) hoặc thu hẹp (scale down) để đảm bảo công việc có thể được xử lý. Một minh họa về hàng đợi công việc tổng quát được thể hiện trong Hình 10-1.

![](images/ch10-123_1.png)

Figure 10-1. A generic work queue

> Hình 10-1. Một hàng đợi công việc tổng quát

## A Generic Work Queue System — Một hệ thống hàng đợi công việc tổng quát

The work queue is an ideal way to demonstrate the power of **distributed system** patterns. Most of the logic in the work queue is wholly independent of the actual work being done, and in many cases the delivery of the work can be performed in an independent manner as well. To illustrate this point, consider the work queue illustrated in Figure 10-1 . If we take a look at it again and identify functionality that can be provided by a shared set of library containers , it becomes apparent that most of the implementation of a containerized work queue can be shared across a wide variety of users, as shown in Figure 10-2 .

> Hàng đợi công việc là một cách lý tưởng để minh họa sức mạnh của các mẫu hệ phân tán. Phần lớn logic trong hàng đợi công việc hoàn toàn độc lập với công việc thực tế đang được thực hiện, và trong nhiều trường hợp, việc cung cấp công việc cũng có thể được thực hiện một cách độc lập. Để minh họa điểm này, hãy xem xét hàng đợi công việc được minh họa trong Hình 10-1. Nếu nhìn lại nó và xác định những chức năng có thể được cung cấp bởi một tập hợp các container thư viện (library) dùng chung, thì rõ ràng là phần lớn việc triển khai một hàng đợi công việc đóng gói container có thể được chia sẻ cho nhiều loại người dùng khác nhau, như thể hiện trong Hình 10-2.

109

> 109

![](images/ch10-124_1.png)

Figure 10-2. The same work queue as shown in Figure 10-1 , but this time using reusable containers. The reusable system containers are shown in white while the user-supplied **container** is shown is grey/blue.

> Hình 10-2. Cùng một hàng đợi công việc như trong Hình 10-1, nhưng lần này sử dụng các container tái sử dụng được. Các container hệ thống tái sử dụng được thể hiện màu trắng còn container do người dùng cung cấp được thể hiện màu xám/xanh.

Building a reusable container-based work queue requires the definition of interfaces between the generic library containers and the user-defined application logic. In the containerized work queue, there are two interfaces: the **source container interface**, which provides a stream of work items that need processing, and the **worker container interface**, which knows how to actually process a **work item**.

> Việc xây dựng một hàng đợi công việc dựa trên container tái sử dụng được đòi hỏi phải định nghĩa các giao diện giữa các container thư viện tổng quát và logic ứng dụng do người dùng định nghĩa. Trong hàng đợi công việc đóng gói container, có hai giao diện: giao diện container nguồn (source container interface), cung cấp một luồng các đơn vị công việc cần xử lý, và giao diện container worker (worker container interface), vốn biết cách thực sự xử lý một đơn vị công việc.

## The Source Container Interface — Giao diện container nguồn

To operate, every work queue needs a collection of work items that need processing. There are many different sources of work items the work queue, depending on the specific application of the work queue. However, once the set of items has been obtained, the actual operation of the work queue is quite generic. Consequently, we can separate the application-specific queue source logic from the generic queue processing logic. Given the previously defined patterns of container groups, this can be seen as an example of the **ambassador** pattern defined previously. The generic work queue container is the primary application container, and the application-specific source container is the ambassador that proxies the generic work queue’s requests out to the concrete definition of the work queue out in the real world. This container group is illustrated in Figure 10-3 .

> Để hoạt động, mọi hàng đợi công việc đều cần một tập hợp các đơn vị công việc cần xử lý. Có nhiều nguồn cung cấp đơn vị công việc khác nhau cho hàng đợi công việc, tùy thuộc vào ứng dụng cụ thể của hàng đợi công việc. Tuy nhiên, một khi tập hợp các đơn vị đã được lấy về, thì hoạt động thực tế của hàng đợi công việc lại khá tổng quát. Do đó, ta có thể tách logic nguồn hàng đợi đặc thù theo ứng dụng ra khỏi logic xử lý hàng đợi tổng quát. Với các mẫu nhóm container đã định nghĩa trước đó, điều này có thể được xem là một ví dụ của mẫu ambassador đã định nghĩa trước đây. Container hàng đợi công việc tổng quát là container ứng dụng chính, còn container nguồn đặc thù theo ứng dụng là ambassador làm trung gian (proxy) chuyển các yêu cầu của hàng đợi công việc tổng quát ra tới định nghĩa cụ thể của hàng đợi công việc ngoài thế giới thực. Nhóm container này được minh họa trong Hình 10-3.

![](images/ch10-124_2.png)

Figure 10-3. The work queue container group Interestingly, while the ambassador container is clearly application-specific, there is also a variety of generic implementations of the work queue source **API**. For example, a source might be a list of pictures stored in a cloud storage API, a collection of files stored on network storage, or a queue in an pub/sub system like Kafka or **Redis**. In these cases, though the user chooses the particular work queue ambassador that fits their scenario, they should be reusing a single generic “library” implementation of the container itself. This minimizes work and maximizes code reuse.

> Hình 10-3. Nhóm container hàng đợi công việc. Điều thú vị là, mặc dù container ambassador rõ ràng là đặc thù theo ứng dụng, vẫn có một loạt các cách triển khai tổng quát cho API nguồn hàng đợi công việc. Ví dụ, một nguồn có thể là danh sách hình ảnh lưu trong một API lưu trữ đám mây, một tập hợp các tệp lưu trên bộ lưu trữ mạng, hoặc một hàng đợi trong hệ thống pub/sub như Kafka hay Redis. Trong những trường hợp này, dù người dùng chọn ambassador hàng đợi công việc cụ thể phù hợp với kịch bản của họ, họ vẫn nên tái sử dụng một cách triển khai "thư viện" tổng quát duy nhất của chính container đó. Điều này giảm thiểu công sức và tối đa hóa việc tái sử dụng code.

Work queue API

> API hàng đợi công việc

Given this coordination between the generic work-queue manager and the application-specific ambassador, we need a formal definition of the **interface** between the two containers. Though there are a variety of different protocols, an HTTP RESTful API is both the easiest to implement as well as the de facto standard for such an interface. The master work queue expects the ambassador to implement the following URLs:

> Với sự phối hợp này giữa trình quản lý hàng đợi công việc tổng quát và ambassador đặc thù theo ứng dụng, ta cần một định nghĩa chính thức cho giao diện giữa hai container. Mặc dù có nhiều giao thức khác nhau, một API HTTP RESTful vừa dễ triển khai nhất vừa là tiêu chuẩn trên thực tế (de facto) cho loại giao diện như vậy. Trình quản lý hàng đợi công việc chính (master) kỳ vọng ambassador triển khai các URL sau:

- GET http://localhost/api/v1/items
- GET http://localhost/api/v1/items/<item-name>

> - GET http://localhost/api/v1/items
> - GET http://localhost/api/v1/items/<item-name>

![](images/ch10-125_1.png)

You might wonder why we include a v1 in the API definition. Will there ever be a v2 of this interface? It may not seem logical, but it costs very little to version your API when you initially define it. Refactoring versioning onto an API without it, on the other hand, is very expensive. Consequently, it is a best practice to always add versions to your APIs even if you’re not sure they will ever change. Better safe than sorry.

> Bạn có thể tự hỏi vì sao ta lại đưa v1 vào trong định nghĩa API. Liệu có bao giờ xuất hiện v2 của giao diện này không? Nghe có vẻ không hợp lý, nhưng việc đánh phiên bản (version) cho API ngay khi mới định nghĩa nó tốn rất ít chi phí. Ngược lại, việc tái cấu trúc để thêm phiên bản vào một API vốn không có lại rất tốn kém. Do đó, một thực hành tốt nhất là luôn thêm phiên bản cho API ngay cả khi bạn không chắc chúng sẽ thay đổi. Cẩn tắc vô áy náy.

The /items/ URL returns a complete list of all items:

> URL /items/ trả về danh sách đầy đủ tất cả các đơn vị:

```
{
   kind: ItemList,
   apiVersion: v1,
   items: [
      “item-1”,
      “item-2”,
      ….
   ]
}
```

The /items/<item-name> URL provides the details for a specific item:

> URL /items/<item-name> cung cấp chi tiết cho một đơn vị cụ thể:

```
{
  kind: Item,
  apiVersion: v1,
  data: {
```

```
      “some”: “json”,
    “object”: “here”,
  }
}
```

Importantly, you will notice that this API doesn’t have any affordances for recording that a work item has been processed. We could have designed a more complicated API and then pushed more implementation into the ambassador container, but remember, the goal of this effort is to place as much of the generic implementation inside of the generic work queue manager as possible. To that end, the work queue manager itself is responsible for tracking which items have been processed and which items remain to be processed.

> Quan trọng là, bạn sẽ nhận thấy API này không có bất kỳ cơ chế nào để ghi nhận rằng một đơn vị công việc đã được xử lý. Ta có thể đã thiết kế một API phức tạp hơn rồi đẩy thêm phần triển khai vào container ambassador, nhưng hãy nhớ rằng, mục tiêu của nỗ lực này là đặt càng nhiều phần triển khai tổng quát vào bên trong trình quản lý hàng đợi công việc tổng quát càng tốt. Vì lẽ đó, chính trình quản lý hàng đợi công việc chịu trách nhiệm theo dõi những đơn vị nào đã được xử lý và những đơn vị nào còn chờ xử lý.

The item details are obtained from this API and the item.data field is passed along to the **worker** interface for processing.

> Chi tiết đơn vị được lấy về từ API này và trường item.data được chuyển tiếp sang giao diện worker để xử lý.

## The Worker Container Interface — Giao diện container worker

Once a particular work item has been obtained by the work queue manager, it needs to be processed by a worker. This is the second container interface in our generic work queue. This container and interface are slightly different than the previous work queue source interface for a few reasons. The first is that it is a one-off API: a single call is made to begin the work, and no other API calls are made throughout the life of the worker container. Secondly, the worker container is not inside a container group with the work queue manager. Instead, it is launched via a container orchestration API and scheduled to its own container group. This means that the work queue manager has to make a remote call to the worker container in order to start work. It also means that we may need to be more careful about security to prevent a malicious user in our cluster from injecting extra work into the system.

> Một khi một đơn vị công việc cụ thể đã được trình quản lý hàng đợi công việc lấy về, nó cần được một worker xử lý. Đây là giao diện container thứ hai trong hàng đợi công việc tổng quát của chúng ta. Container và giao diện này hơi khác so với giao diện nguồn hàng đợi công việc trước đó vì một vài lý do. Thứ nhất, đây là một API dùng một lần (one-off): chỉ một lời gọi duy nhất được thực hiện để bắt đầu công việc, và không có lời gọi API nào khác trong suốt vòng đời của container worker. Thứ hai, container worker không nằm trong cùng một nhóm container với trình quản lý hàng đợi công việc. Thay vào đó, nó được khởi chạy thông qua một API điều phối container và được lập lịch vào nhóm container riêng của nó. Điều này có nghĩa là trình quản lý hàng đợi công việc phải thực hiện một lời gọi từ xa tới container worker để bắt đầu công việc. Nó cũng có nghĩa là ta có thể cần cẩn trọng hơn về bảo mật để ngăn một người dùng độc hại trong cụm (cluster) của chúng ta chèn thêm công việc vào hệ thống.

With the work queue source API, we used a simple HTTP-based API for sending items back to the work queue manager. This was because we needed to make repeated calls to the API, and security wasn’t a concern since everything was running on localhost. With the worker container, we only need to make a single call, and we want to ensure that other users in the system can’t accidentally or maliciously add work to our workers. Consequently, for the worker container, we will use a file-based API. Namely, when the worker container is created, it will receive an environment variable named WORK_ITEM_FILE ; this will point to a file in the container’s local filesystem, where the data field from a work item has been written to a file. Concretely, as you will see below, this API can be implemented via a **Kubernetes** **ConfigMap** object that can be mounted into a container group as a file, as illustrated in Figure 10-4 .

> Với API nguồn hàng đợi công việc, ta đã dùng một API dựa trên HTTP đơn giản để gửi các đơn vị trở lại trình quản lý hàng đợi công việc. Đó là vì ta cần thực hiện các lời gọi lặp đi lặp lại tới API, và bảo mật không phải là mối lo vì mọi thứ đều chạy trên localhost. Với container worker, ta chỉ cần thực hiện một lời gọi duy nhất, và ta muốn đảm bảo rằng những người dùng khác trong hệ thống không thể vô tình hay cố ý thêm công việc vào các worker của ta. Do đó, với container worker, ta sẽ dùng một API dựa trên tệp (file-based). Cụ thể, khi container worker được tạo, nó sẽ nhận một biến môi trường tên là WORK_ITEM_FILE; biến này sẽ trỏ tới một tệp trong hệ thống tệp cục bộ của container, nơi trường data của một đơn vị công việc đã được ghi ra tệp. Cụ thể hơn, như bạn sẽ thấy bên dưới, API này có thể được triển khai thông qua một đối tượng ConfigMap của Kubernetes vốn có thể được gắn (mount) vào một nhóm container dưới dạng tệp, như minh họa trong Hình 10-4.

![](images/ch10-127_1.png)

Figure 10-4. The work queue worker API

> Hình 10-4. API worker của hàng đợi công việc

This file-based API pattern is also easier for the container to implement. Often a work queue worker is simply a shell script across a few command line tools. In that context, spinning up a web server to manage the work to perform is an unnecessary complexity. As was true with the work queue source implementation, most of the worker containers will be special-purpose container images built for specific work queue applications, but there are also generic workers that can be applied to multiple different work queue applications.

> Mẫu API dựa trên tệp này cũng dễ triển khai hơn cho container. Thường thì một worker hàng đợi công việc chỉ đơn giản là một shell script gọi qua vài công cụ dòng lệnh. Trong bối cảnh đó, việc dựng lên một web server để quản lý công việc cần thực hiện là một sự phức tạp không cần thiết. Cũng giống như với cách triển khai nguồn hàng đợi công việc, phần lớn các container worker sẽ là các image container chuyên dụng được xây dựng cho những ứng dụng hàng đợi công việc cụ thể, nhưng cũng có những worker tổng quát có thể được áp dụng cho nhiều ứng dụng hàng đợi công việc khác nhau.

Consider the example of a work queue worker that downloads a file from cloud storage and runs a shell script with that file as input, and then copies its output back up to cloud storage. Such a container can be mostly generic but then have the specific script to execute supplied to it as a runtime parameter. In this way, most of the work of file handling can be shared by multiple users/work queues and only the specifics of file processing need to be supplied by the end user.

> Hãy xem xét ví dụ về một worker hàng đợi công việc tải một tệp từ bộ lưu trữ đám mây và chạy một shell script với tệp đó làm đầu vào, rồi sao chép đầu ra trở lại lên bộ lưu trữ đám mây. Một container như vậy có thể phần lớn là tổng quát nhưng sau đó được cung cấp script cụ thể cần thực thi như một tham số lúc chạy (runtime parameter). Theo cách này, phần lớn công việc xử lý tệp có thể được nhiều người dùng/hàng đợi công việc chia sẻ, và chỉ những phần cụ thể của việc xử lý tệp mới cần do người dùng cuối cung cấp.

## The Shared Work Queue Infrastructure — Hạ tầng hàng đợi công việc dùng chung

Given implementations of the two container interfaces described previously, what is left to implement our reusable work queue implementation? The basic algorithm for the work queue is fairly straightforward:

> Với các cách triển khai của hai giao diện container đã mô tả trước đó, còn lại điều gì cần triển khai cho hàng đợi công việc tái sử dụng được của chúng ta? Thuật toán cơ bản của hàng đợi công việc khá đơn giản:

1. Load the available work by calling into source container interface.
2. Consult with work queue state to determine which work items have been processed or are being processed currently.
3. For these items, spawn jobs that use the worker container interface to process the work item.
4. When one of these worker containers finishes successfully, record that the work item has been completed.

> 1. Nạp các công việc khả dụng bằng cách gọi vào giao diện container nguồn.
> 2. Tham chiếu với trạng thái hàng đợi công việc để xác định những đơn vị công việc nào đã được xử lý hoặc đang được xử lý hiện tại.
> 3. Với các đơn vị này, sinh ra các job dùng giao diện container worker để xử lý đơn vị công việc.
> 4. Khi một trong các container worker này hoàn thành thành công, ghi nhận rằng đơn vị công việc đã hoàn tất.

While this algorithm is simple to express in words, it is somewhat more complicated to implement in reality. Fortunately for us, the Kubernetes **container orchestrator** contains a number of features that make it significantly easier to implement. Namely, Kubernetes contains a **Job** object that allows for the reliable execution of the work queue. The Job can be configured to either run the worker container once or to run it until it completes successfully. If the worker container is set to run to completion, then even if a machine in the cluster fails, the job will eventually be run to success. This dramatically simplifies the task of building a work queue because the orchestrator takes on responsibility for the reliable operation of each work item.

> Mặc dù thuật toán này dễ diễn đạt bằng lời, việc triển khai nó trong thực tế lại có phần phức tạp hơn. May mắn cho chúng ta, bộ điều phối container Kubernetes chứa một số tính năng giúp việc triển khai dễ dàng hơn đáng kể. Cụ thể, Kubernetes chứa một đối tượng Job cho phép thực thi hàng đợi công việc một cách tin cậy. Job có thể được cấu hình để hoặc chạy container worker một lần, hoặc chạy nó cho đến khi hoàn thành thành công. Nếu container worker được đặt chạy đến khi hoàn tất, thì ngay cả khi một máy trong cụm gặp sự cố, job rồi cũng sẽ được chạy đến thành công. Điều này đơn giản hóa đáng kể nhiệm vụ xây dựng một hàng đợi công việc vì bộ điều phối đảm nhận trách nhiệm về hoạt động tin cậy của mỗi đơn vị công việc.

Additionally, Kubernetes has annotations for each Job object that enable us to mark each job with the work item it is processing. This enables us to understand which items are being processed as well as those that have completed in either failure or success.

> Ngoài ra, Kubernetes có các chú thích (annotation) cho mỗi đối tượng Job cho phép ta đánh dấu mỗi job với đơn vị công việc mà nó đang xử lý. Điều này cho phép ta hiểu được những đơn vị nào đang được xử lý cũng như những đơn vị nào đã hoàn tất, dù thất bại hay thành công.

Put together, this means that we can implement a work queue on top of the Kubernetes orchestration layer without using any storage of our own. This dramatically simplifies the task of building the work queue infrastructure.

> Gộp lại, điều này có nghĩa là ta có thể triển khai một hàng đợi công việc trên nền tầng điều phối Kubernetes mà không cần dùng bất kỳ kho lưu trữ nào của riêng mình. Điều này đơn giản hóa đáng kể nhiệm vụ xây dựng hạ tầng hàng đợi công việc.

Thus, the expanded operation of our work queue container looks like this:

> Như vậy, hoạt động mở rộng của container hàng đợi công việc của chúng ta trông như sau:

```
Repeat forever
```

Get the list of work items from the work source container interface. Get the list of all jobs that have been created to **service** this work queue. Difference these lists to find the set of work items that haven’t been processed.

> Lấy danh sách các đơn vị công việc từ giao diện container nguồn công việc. Lấy danh sách tất cả các job đã được tạo để phục vụ hàng đợi công việc này. Lấy hiệu (difference) của hai danh sách này để tìm ra tập các đơn vị công việc chưa được xử lý.

For these unprocessed items, create new Job objects that spawn the appropriate worker container.

> Với những đơn vị chưa xử lý này, tạo các đối tượng Job mới sinh ra container worker thích hợp.

Here is a simple Python script that implements this work queue:

> Đây là một script Python đơn giản triển khai hàng đợi công việc này:

```
import requests
import json
from kubernetes import client, config
import time
namespace = "default"
def make_container(item, obj):
    container = client.V1Container()
    container.image = "my/worker-image"
    container.name = "worker"
    return container
def make_job(item):
    response = requests.get("http://localhost:8000/items/{}".format(item))
    obj = json.loads(response.text)
    job = client.V1Job()
    job.metadata = client.V1ObjectMeta()
    job.metadata.name = item
```

```
    job.spec = client.V1JobSpec()
    job.spec.template = client.V1PodTemplate()
    job.spec.template.spec = client.V1PodTemplateSpec()
    job.spec.template.spec.restart_policy = "Never"
    job.spec.template.spec.containers = [
        make_container(item, obj)
    ]
    return job
def update_queue(batch):
    response = requests.get("http://localhost:8000/items")
    obj = json.loads(response.text)
    items = obj['items']
    ret = batch.list_namespaced_job(namespace, watch=False)
    for item in items:
        found = False
        for i in ret.items:
            if i.metadata.name == item:
                found = True
        if not found:
            # This function creates the job object, omitted for
            # brevity
            job = make_job(item)
            batch.create_namespaced_job(namespace, job)
config.load_kube_config()
batch = client.BatchV1Api()
while True:
    update_queue(batch)
    time.sleep(10)
```

## Hands On: Implementing a Video Thumbnailer — Thực hành (Hands On): Triển khai bộ tạo thumbnail video

To provide a concrete example of how we might use a work queue, consider the task of generating thumbnails for videos. These thumbnails help users determine which videos they want to watch.

> Để đưa ra một ví dụ cụ thể về cách ta có thể dùng một hàng đợi công việc, hãy xem xét nhiệm vụ tạo thumbnail cho các video. Những thumbnail này giúp người dùng xác định họ muốn xem video nào.

To implement this video thumbnailer, we need two different user containers. The first is the work item source container. The simplest way for this to work is for the work items to appear on a shared disk, such as a Network File System (NFS) share. The work item source simply lists the files in this directory and returns them to the caller. Here’s a simple **node** program that does this:

> Để triển khai bộ tạo thumbnail video này, ta cần hai container người dùng khác nhau. Cái thứ nhất là container nguồn đơn vị công việc. Cách đơn giản nhất để nó hoạt động là các đơn vị công việc xuất hiện trên một đĩa dùng chung, chẳng hạn một chia sẻ Network File System (NFS). Nguồn đơn vị công việc chỉ đơn giản liệt kê các tệp trong thư mục này và trả chúng về cho phía gọi. Đây là một chương trình node đơn giản làm điều đó:

```
const http = require('http');
const fs = require('fs');
const port = 8080;
```

```
const path = process.env.MEDIA_PATH;
const requestHandler = (request, response) => {
console.log(request.url);
fs.readdir(path + '/*.mp4', (err, items) => {
var msg = {
'kind': 'ItemList',
'apiVersion': 'v1',
'items': []
};
if (!items) {
return msg;
}
for (var i = 0; i < items.length; i++) {
msg.items.push(items[i]);
}
response.end(JSON.stringify(msg));
});
}
const server = http.createServer(requestHandler);
server.listen(port, (err) => {
if (err) {
return console.log('Error starting server', err);
}
console.log(`server is active on ${port}`)
});
```

This source of defines the queue of movies to **thumbnail**. We use the ffmpeg utility to actually perform the thumbnailing work.

> Nguồn này định nghĩa hàng đợi các bộ phim cần tạo thumbnail. Ta dùng tiện ích ffmpeg để thực sự thực hiện công việc tạo thumbnail.

You can create a container that uses the following as its command line:

> Bạn có thể tạo một container dùng đoạn sau làm dòng lệnh của nó:

```
ffmpeg -i ${INPUT_FILE} -frames:v 100 thumb.png
```

This command will take one frame every 100 frames (that’s the -frames:v 100 flag) and output it as a PNG file (e.g., thumb1.png , thumb2.png , etc.).

> Lệnh này sẽ lấy một khung hình mỗi 100 khung hình (đó là cờ -frames:v 100) và xuất nó ra dưới dạng tệp PNG (ví dụ thumb1.png, thumb2.png, v.v.).

You can implement this image processing using an existing ffmpeg **Docker** image. The jrottenberg/ffmpeg Docker image is a popular choice.

> Bạn có thể triển khai việc xử lý hình ảnh này bằng một image Docker ffmpeg có sẵn. Image Docker jrottenberg/ffmpeg là một lựa chọn phổ biến.

By defining a simple source container as well as an even simpler worker container, we can clearly see the power and utility of a generic, container-based queuing system. It dramatically reduces the time/distance between an idea for implementing a work queue and the corresponding concrete implementation.

> Bằng cách định nghĩa một container nguồn đơn giản cũng như một container worker còn đơn giản hơn nữa, ta có thể thấy rõ sức mạnh và tính tiện ích của một hệ thống hàng đợi dựa trên container tổng quát. Nó giảm đáng kể khoảng cách thời gian/quãng đường giữa một ý tưởng triển khai hàng đợi công việc và cách triển khai cụ thể tương ứng.

## Dynamic Scaling of the Workers — Co giãn động số lượng worker

The previously described work queue is great for processing work items as quickly as they arrive in the work queue, but this can lead to bursty resource loads being placed onto a container orchestrator cluster. This is good if you have a lot of different workloads that will burst at different times and thus keep your infrastructure evenly utilized. But if you don’t have a sufficient number of different workloads, this **feast or famine** approach to scaling your work queue might require that you over-provision resources to support the bursts that will lay idle (and cost too much money) while you don’t have work to perform.

> Hàng đợi công việc đã mô tả trước đó rất tuyệt cho việc xử lý các đơn vị công việc nhanh ngay khi chúng đến hàng đợi, nhưng điều này có thể dẫn đến những tải tài nguyên dồn dập (bursty) đặt lên cụm bộ điều phối container. Điều này tốt nếu bạn có nhiều khối lượng công việc (workload) khác nhau dồn lên vào những thời điểm khác nhau, qua đó giữ cho hạ tầng của bạn được sử dụng đều đặn. Nhưng nếu bạn không có đủ số lượng workload khác nhau, thì cách tiếp cận co giãn kiểu "no dồn đói góp" này có thể đòi hỏi bạn phải cấp dư (over-provision) tài nguyên để hỗ trợ các đợt dồn, vốn sẽ nằm không (và tốn quá nhiều tiền) trong lúc bạn không có việc để làm.

To address this problem, you can limit the overall number of Job objects that your work queue is willing to create. This will naturally serve to limit the number of work items you process in parallel and consequentially limit the maximum amount of resources that you use at a particular time. However, doing this will increase the time to completion (**latency**) for each work item being completed when under heavy load. If the load is bursty, then this is probably okay because you can use the slack times to catch up with the backlog that developed during a burst of usage. However, if your steady-state usage is too high, your work queue may never be able to catch up and the time to completion will simply get longer and longer.

> Để giải quyết vấn đề này, bạn có thể giới hạn tổng số đối tượng Job mà hàng đợi công việc của bạn sẵn sàng tạo ra. Điều này một cách tự nhiên sẽ giúp giới hạn số đơn vị công việc bạn xử lý song song, và do đó giới hạn lượng tài nguyên tối đa mà bạn sử dụng tại một thời điểm cụ thể. Tuy nhiên, làm như vậy sẽ tăng thời gian hoàn thành (độ trễ) cho mỗi đơn vị công việc khi hệ thống chịu tải nặng. Nếu tải có tính dồn dập, thì điều này có lẽ ổn vì bạn có thể dùng những lúc rảnh để bắt kịp lượng tồn đọng (backlog) phát sinh trong một đợt sử dụng dồn dập. Tuy nhiên, nếu mức sử dụng ở trạng thái ổn định (steady-state) của bạn quá cao, thì hàng đợi công việc của bạn có thể sẽ không bao giờ bắt kịp và thời gian hoàn thành sẽ ngày càng dài ra.

When your work queue is faced with this situation, you need to have it dynamically adjust itself to increase the parallelism that it is willing to create (and correspondingly the resources it is willing to use) so that it can keep up with the incoming work. Fortunately, there are mathematical formulas that we can use to determine when we need to dynamically scale up our work queue.

> Khi hàng đợi công việc của bạn đối mặt với tình huống này, bạn cần để nó tự điều chỉnh động nhằm tăng mức song song mà nó sẵn sàng tạo ra (và tương ứng là tài nguyên nó sẵn sàng sử dụng) để có thể theo kịp lượng công việc đến. May mắn thay, có những công thức toán học mà ta có thể dùng để xác định khi nào ta cần mở rộng động hàng đợi công việc.

Consider a work queue where a new work item arrives an average of once every minute, and each work item takes an average of 30 seconds to complete. Such a system is capable of keeping up with all of the work it receives. Even if a large batch of work arrives all at once and creates a backlog, on average the work queue processes two work items for every one work item that arrives, and thus it will be able to gradually work through its backlog.

> Hãy xem xét một hàng đợi công việc trong đó một đơn vị công việc mới đến trung bình một lần mỗi phút, và mỗi đơn vị công việc mất trung bình 30 giây để hoàn thành. Một hệ thống như vậy có khả năng theo kịp toàn bộ công việc nó nhận được. Ngay cả khi một lô lớn công việc đến cùng lúc và tạo ra lượng tồn đọng, thì trung bình hàng đợi công việc xử lý hai đơn vị công việc cho mỗi một đơn vị công việc đến, và do đó nó sẽ có thể dần dần giải quyết hết lượng tồn đọng của mình.

If, instead, a new work item arrives on average once every minute and it takes an average of one minute to process each work item, then the system is perfectly balanced, but it does not handle variance well. It can catch up with bursts—but it will take a while, and it has no slack or capacity to absorb a sustained increase in the rate at which new work items arrive. This is probably not an ideal way to run, as some safety margin for growth and other sustained increases in work (or unexpected slowdowns in processing) is needed to preserve a stable system.

> Ngược lại, nếu một đơn vị công việc mới đến trung bình một lần mỗi phút và mất trung bình một phút để xử lý mỗi đơn vị, thì hệ thống cân bằng hoàn hảo, nhưng nó không xử lý tốt biến thiên (variance). Nó có thể bắt kịp các đợt dồn — nhưng sẽ mất một lúc, và nó không có dư địa hay năng lực để hấp thụ một sự gia tăng kéo dài về tốc độ đến của các đơn vị công việc mới. Đây có lẽ không phải là cách vận hành lý tưởng, vì cần có một biên độ an toàn nào đó cho tăng trưởng và các gia tăng kéo dài khác về công việc (hoặc những lần chậm trễ xử lý ngoài dự kiến) để duy trì một hệ thống ổn định.

Finally, consider a system in which a work item arrives every minute and each item takes 2 minutes to process. In such a system, we are always losing ground. The queue of work will grow without bound and the latency of any one item in the queue will grow toward infinity (and our users will become very frustrated).

> Cuối cùng, hãy xem xét một hệ thống trong đó một đơn vị công việc đến mỗi phút và mỗi đơn vị mất 2 phút để xử lý. Trong một hệ thống như vậy, ta luôn bị thụt lùi. Hàng đợi công việc sẽ tăng không giới hạn và độ trễ của bất kỳ đơn vị nào trong hàng đợi sẽ tăng dần đến vô cùng (và người dùng của chúng ta sẽ trở nên cực kỳ bực bội).

Thus, we can keep track of both of these metrics for our work queue, and the average time between work items over an extended period of time (# work items / 24 hours) will give us the **interarrival time** for new work. We can also keep track of the average time to process any one item once we start working on it (not counting any time in the queue). To have a stable work queue, we need to adjust the number of resources so that the time to process any item is less than the interarrival time of new items. If we are processing work items in parallel, we also divide the processing time for a work item by the parallelism. For example, if each item takes one minute to process but we process four items in parallel, the effective time to process one item is 15 seconds, and thus we can sustain an interarrival period of 16 or more seconds.

> Như vậy, ta có thể theo dõi cả hai chỉ số này cho hàng đợi công việc của mình, và thời gian trung bình giữa các đơn vị công việc trong một khoảng thời gian dài (# đơn vị công việc / 24 giờ) sẽ cho ta thời gian giữa các lần đến (interarrival time) của công việc mới. Ta cũng có thể theo dõi thời gian trung bình để xử lý bất kỳ một đơn vị nào sau khi ta bắt đầu làm việc với nó (không tính thời gian nằm trong hàng đợi). Để có một hàng đợi công việc ổn định, ta cần điều chỉnh số lượng tài nguyên sao cho thời gian xử lý bất kỳ đơn vị nào nhỏ hơn thời gian giữa các lần đến của các đơn vị mới. Nếu ta đang xử lý các đơn vị công việc song song, ta cũng chia thời gian xử lý của một đơn vị công việc cho mức song song. Ví dụ, nếu mỗi đơn vị mất một phút để xử lý nhưng ta xử lý bốn đơn vị song song, thì thời gian xử lý hiệu dụng của một đơn vị là 15 giây, và do đó ta có thể duy trì một khoảng thời gian giữa các lần đến từ 16 giây trở lên.

This approach makes it fairly straightforward to build an **autoscaler** to dynamically size up our work queue. Sizing down the work queue is somewhat trickier, but you can use the same math as well as a heuristic for the amount of spare capacity for the safety margin you want to maintain. For example, you can **reduce** the parallelism until the processing time for an item is 90% of the interarrival time for new items.

> Cách tiếp cận này khiến cho việc xây dựng một bộ tự co giãn (autoscaler) để định cỡ động hàng đợi công việc trở nên khá đơn giản. Việc thu nhỏ hàng đợi công việc có phần khó hơn một chút, nhưng bạn có thể dùng cùng phép toán cũng như một heuristic về lượng năng lực dự phòng cho biên độ an toàn mà bạn muốn duy trì. Ví dụ, bạn có thể giảm mức song song cho đến khi thời gian xử lý một đơn vị bằng 90% thời gian giữa các lần đến của các đơn vị mới.

## The Multi-Worker Pattern — Mẫu đa worker

One of the themes of this book has been the use of containers for encapsulation and reuse of code. The same holds true for the work queue patterns described in this chapter. In addition to the patterns for reusing containers for driving the work queue itself, you can also reuse multiple different containers to compose a worker implementation. Suppose, for example, that you have three different types of work that you want to perform on a particular work queue item. For example, you might want to detect faces in an image, tag those faces with identities, and then blur the faces in the image. You could write a single worker to perform this complete set of tasks, but this would be a bespoke solution that would not be reusable the next time you want to identify something else, such as cars, yet still provide the same blurring.

> Một trong những chủ đề xuyên suốt cuốn sách này là việc dùng container để đóng gói và tái sử dụng code. Điều tương tự cũng đúng với các mẫu hàng đợi công việc được mô tả trong chương này. Ngoài các mẫu để tái sử dụng container cho việc vận hành chính hàng đợi công việc, bạn cũng có thể tái sử dụng nhiều container khác nhau để hợp thành một cách triển khai worker. Giả sử, ví dụ, bạn có ba loại công việc khác nhau mà bạn muốn thực hiện trên một đơn vị công việc cụ thể của hàng đợi. Chẳng hạn, bạn có thể muốn phát hiện khuôn mặt trong một ảnh, gắn nhãn danh tính cho các khuôn mặt đó, rồi làm mờ các khuôn mặt trong ảnh. Bạn có thể viết một worker duy nhất để thực hiện trọn bộ các tác vụ này, nhưng đó sẽ là một giải pháp đo ni đóng giày không tái sử dụng được vào lần tiếp theo khi bạn muốn nhận dạng thứ gì khác, chẳng hạn xe hơi, mà vẫn cung cấp cùng tính năng làm mờ.

To achieve this kind of code reuse, the **multi-worker pattern** is something of a specialization of the **adapter** pattern described in previous chapters. In this case, the multi-worker pattern transforms a collection of different worker containers into a single unified container that implements the worker interface, yet delegates the actual work to a collection of different, reusable containers. This process is illustrated in Figure 10-5 .

> Để đạt được kiểu tái sử dụng code này, mẫu đa worker (multi-worker) phần nào là một dạng đặc biệt hóa của mẫu adapter đã mô tả ở các chương trước. Trong trường hợp này, mẫu đa worker biến đổi một tập hợp các container worker khác nhau thành một container hợp nhất duy nhất triển khai giao diện worker, nhưng lại ủy thác công việc thực tế cho một tập hợp các container khác nhau, tái sử dụng được. Quá trình này được minh họa trong Hình 10-5.

![](images/ch10-133_1.png)

Figure 10-5. The multi-worker aggregator pattern as a group of containers

> Hình 10-5. Mẫu tổng hợp đa worker dưới dạng một nhóm container

Because of this code reuse, the composition of multiple different worker containers means an increase in the reuse of code and a reduction in effort for people designing batch-oriented distributed systems.

> Nhờ việc tái sử dụng code này, việc hợp thành nhiều container worker khác nhau đồng nghĩa với việc gia tăng mức tái sử dụng code và giảm công sức cho những người thiết kế các hệ phân tán hướng theo lô.
