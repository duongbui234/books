# CHAPTER 7 Scatter/Gather — CHAPTER 7 Scatter/Gather

So far we’ve examined systems that replicate for **scalability** in terms of the number of requests processed per second (the **stateless** replicated pattern), as well as scalability for the size of the data (the sharded data pattern). In this chapter we introduce the **scatter**/**gather** pattern, which uses replication for scalability in terms of time. Specifically, the scatter/gather pattern allows you to achieve parallelism in servicing requests, enabling you to **service** them significantly faster than you could if you had to service them sequentially.

> Cho đến giờ chúng ta đã khảo sát các hệ thống nhân bản (replicate) để mở rộng theo số lượng yêu cầu xử lý được mỗi giây (mẫu nhân bản không trạng thái), cũng như mở rộng theo kích thước dữ liệu (mẫu dữ liệu phân mảnh). Trong chương này chúng ta giới thiệu mẫu scatter/gather, vốn dùng nhân bản để mở rộng theo chiều thời gian. Cụ thể, mẫu scatter/gather cho phép bạn đạt được tính song song khi phục vụ các yêu cầu, nhờ đó phục vụ chúng nhanh hơn đáng kể so với khi phải xử lý tuần tự.

Like replicated and sharded systems, the scatter/gather pattern is a tree pattern with a root that distributes requests and leaves that process those requests. However, in contrast to replicated and sharded systems, with scatter/gather requests are simultaneously farmed out to all of the replicas in the system. Each **replica** does a small amount of processing and then returns a fraction of the result to the root. The root server then combines the various partial results together to form a single complete response to the request and then sends this request back out to the client. The scatter/gather pattern is illustrated in Figure 7-1 .

> Giống như các hệ thống nhân bản và phân mảnh, mẫu scatter/gather là một mẫu dạng cây với một gốc (root) phân phối các yêu cầu và các lá (leaf) xử lý những yêu cầu đó. Tuy nhiên, khác với các hệ thống nhân bản và phân mảnh, với scatter/gather thì các yêu cầu được phân phát đồng thời tới tất cả các bản sao (replica) trong hệ thống. Mỗi bản sao xử lý một lượng nhỏ rồi trả về một phần kết quả cho gốc. Sau đó máy chủ gốc kết hợp các kết quả thành phần khác nhau lại với nhau để tạo thành một phản hồi hoàn chỉnh duy nhất cho yêu cầu, rồi gửi phản hồi này trở lại cho client. Mẫu scatter/gather được minh họa trong Hình 7-1.

Scatter/gather is quite useful when you have a large amount of mostly independent processing that is needed to handle a particular request. Scatter/gather can be seen as sharding the computation necessary to service the request, rather than sharding the data (although data sharding may be part of it as well).

> Scatter/gather khá hữu ích khi bạn có một lượng lớn công việc xử lý phần lớn là độc lập cần thực hiện để phục vụ một yêu cầu cụ thể. Có thể xem scatter/gather như việc phân mảnh phần tính toán cần thiết để phục vụ yêu cầu, thay vì phân mảnh dữ liệu (mặc dù phân mảnh dữ liệu cũng có thể là một phần của nó).

73

> 73

![](images/ch07-88_1.png)

Figure 7-1. A scatter/gather pattern

> Hình 7-1. Một mẫu scatter/gather

## Scatter/Gather with Root Distribution — Scatter/Gather với Phân phối tại Gốc

The simplest form of scatter/gather is one in which each leaf is entirely homogenous but the work is distributed to a number of different leaves in order to improve the performance of the request. This pattern is equivalent to solving an “embarassingly parallel” problem. The problem can be broken up into many different pieces and each piece can be put back together with all of the other pieces to form a complete answer.

> Dạng đơn giản nhất của scatter/gather là dạng trong đó mỗi lá hoàn toàn đồng nhất nhưng công việc được phân phối tới nhiều lá khác nhau nhằm cải thiện hiệu năng của yêu cầu. Mẫu này tương đương với việc giải một bài toán "song song hóa dễ dàng" (embarrassingly parallel). Bài toán có thể được chia thành nhiều phần khác nhau và mỗi phần có thể được ghép lại cùng tất cả các phần khác để tạo thành một câu trả lời hoàn chỉnh.

To understand this in more concrete terms, imagine that you need to service a user request R and it takes one minute for a single core to produce the answer A to this request. If we program a multi-threaded application, we can parallelize this request on a single machine by using multiple cores. Given this approach and a 30 core processor (yes, typically it would be a 32 core processor, but 30 makes the math cleaner), we can **reduce** the time that it takes to process a single request down to 2 seconds (60 seconds of computation split across 30 threads for computation is equal to 2 seconds). But even two seconds is pretty slow to service a user’s web request. Additionally, truly achieving a completely parallel speed up on a single process is going to be tricky as things like memory, network, or disk bandwidth start to become the bottleneck. Instead of parallelizing an application across cores on a single machine, we can use the scatter/gather pattern to parallelize requests across multiple processes on many different machines. In this way, we can improve our overall **latency** requests, since we are no longer bound by the number of cores we can get on a single machine, as well as ensure that the bottleneck in our process continues to be CPU, since the memory, network, and disk bandwidth are all spread across a number of different machines. Additionally, because every machine in the scatter/gather tree is capable of handling every request, the root of the tree can dynamically dispatch load to different nodes at different times depending on their responsiveness. If, for some reason, a particular leaf **node** is responding more slowly than other machines (e.g., it has a noisy neighbor process that is interfering with resources), then the root can dynamically redistribute load to assure a fast response.

> Để hiểu điều này một cách cụ thể hơn, hãy hình dung rằng bạn cần phục vụ một yêu cầu người dùng R và phải mất một phút để một lõi (core) đơn lẻ tạo ra câu trả lời A cho yêu cầu này. Nếu chúng ta lập trình một ứng dụng đa luồng, ta có thể song song hóa yêu cầu này trên một máy duy nhất bằng cách dùng nhiều lõi. Với cách tiếp cận này và một bộ xử lý 30 lõi (vâng, thông thường sẽ là bộ xử lý 32 lõi, nhưng 30 làm phép tính gọn hơn), ta có thể giảm thời gian xử lý một yêu cầu xuống còn 2 giây (60 giây tính toán chia đều cho 30 luồng tính toán bằng 2 giây). Nhưng kể cả hai giây vẫn khá chậm để phục vụ một yêu cầu web của người dùng. Hơn nữa, việc thực sự đạt được tăng tốc song song hoàn toàn trên một tiến trình đơn lẻ sẽ rất khó khăn vì những thứ như bộ nhớ, mạng, hay băng thông đĩa bắt đầu trở thành nút thắt cổ chai. Thay vì song song hóa một ứng dụng trên nhiều lõi của một máy duy nhất, ta có thể dùng mẫu scatter/gather để song song hóa các yêu cầu trên nhiều tiến trình ở nhiều máy khác nhau. Theo cách này, ta có thể cải thiện độ trễ tổng thể của các yêu cầu, vì ta không còn bị giới hạn bởi số lõi có thể có trên một máy duy nhất, đồng thời đảm bảo rằng nút thắt cổ chai trong tiến trình của ta vẫn là CPU, vì bộ nhớ, mạng và băng thông đĩa đều được trải đều trên nhiều máy khác nhau. Ngoài ra, vì mỗi máy trong cây scatter/gather đều có khả năng xử lý mọi yêu cầu, gốc của cây có thể điều phối tải một cách động tới các nút khác nhau vào những thời điểm khác nhau tùy theo mức độ phản hồi của chúng. Nếu vì lý do nào đó một nút lá cụ thể phản hồi chậm hơn các máy khác (ví dụ, nó có một tiến trình "hàng xóm ồn ào" đang can thiệp vào tài nguyên), thì gốc có thể phân phối lại tải một cách động để đảm bảo phản hồi nhanh.

## Hands On: Distributed Document Search — Thực hành (Hands On): Tìm kiếm Tài liệu Phân tán

To see an example of scatter/gather in action, consider the task of searching across a large database of documents for all documents that contain the words “cat” and “dog.” One way to perform this search would be to open up all of the documents, read through the entire set, searching for the words in each document, and then return to the user the set of documents that contain both words.

> Để thấy một ví dụ về scatter/gather trong thực tế, hãy xét tác vụ tìm kiếm trong một cơ sở dữ liệu lớn gồm các tài liệu để tìm tất cả tài liệu chứa các từ "cat" và "dog". Một cách thực hiện việc tìm kiếm này là mở tất cả các tài liệu, đọc qua toàn bộ tập, tìm các từ trong mỗi tài liệu, rồi trả về cho người dùng tập các tài liệu chứa cả hai từ.

As you might imagine, this is quite a slow process because it requires opening and reading through a large number of files for each request. To make request processing faster, you can build an index . The index is effectively a hashtable, where the keys are individual words (e.g., “cat”) and the values are a list of documents containing that word.

> Như bạn có thể hình dung, đây là một quá trình khá chậm vì nó đòi hỏi phải mở và đọc qua một số lượng lớn tệp cho mỗi yêu cầu. Để xử lý yêu cầu nhanh hơn, bạn có thể xây dựng một chỉ mục (index). Chỉ mục thực chất là một bảng băm (hashtable), trong đó các khóa là từng từ riêng lẻ (ví dụ, "cat") và các giá trị là một danh sách các tài liệu chứa từ đó.

Now, instead of searching through every document, finding the documents that match any one word is as easy as doing a lookup in this hashtable. However, we have lost one important ability. Remember that we were looking for all documents that contained “cat” and “dog.” Since the index only has single words, not conjunctions of words, we still need to find the documents that contain both words. Luckily, this is just an intersection of the sets of documents returned for each word.

> Giờ đây, thay vì tìm kiếm qua mọi tài liệu, việc tìm các tài liệu khớp với một từ bất kỳ đơn giản như thực hiện một lần tra cứu trong bảng băm này. Tuy nhiên, ta đã đánh mất một khả năng quan trọng. Hãy nhớ rằng chúng ta đang tìm tất cả các tài liệu chứa cả "cat" và "dog". Vì chỉ mục chỉ có các từ đơn, không phải các kết hợp từ, ta vẫn cần tìm các tài liệu chứa cả hai từ. May thay, đây chỉ là phép giao của các tập tài liệu được trả về cho mỗi từ.

Given this approach, we can implement this document search as an example of the scatter/gather pattern. When a request comes in to the document search root, it parses the request and farms out two leaf machines (one for the word “cat” and one for the word “dog”). Each of these machines returns a list of documents that match one of the words, and the root node returns the list of documents containing both “cat” and “dog.”

> Với cách tiếp cận này, ta có thể triển khai việc tìm kiếm tài liệu này như một ví dụ của mẫu scatter/gather. Khi một yêu cầu đến gốc tìm kiếm tài liệu, nó phân tích yêu cầu và phân phát tới hai máy lá (một cho từ "cat" và một cho từ "dog"). Mỗi máy này trả về một danh sách các tài liệu khớp với một trong hai từ, và nút gốc trả về danh sách các tài liệu chứa cả "cat" và "dog".

A diagram of this process is shown in Figure 7-2 : the leaf returns {doc1, doc2, doc4} for “cat” and {doc1, doc3, doc4} for “dog,” so the root finds the intersection and returns {doc1, doc4} .

> Một sơ đồ của quá trình này được thể hiện trong Hình 7-2: lá trả về {doc1, doc2, doc4} cho "cat" và {doc1, doc3, doc4} cho "dog", nên gốc tìm phép giao và trả về {doc1, doc4}.

![](images/ch07-90_1.png)

Figure 7-2. Example of a term-sharded scatter/gather system

> Hình 7-2. Ví dụ về một hệ thống scatter/gather phân mảnh theo từ khóa

## Scatter/Gather with Leaf Sharding — Scatter/Gather với Phân mảnh tại Lá

While applying the replicated data scatter/gather pattern allows you to reduce the processing time required for handling user requests, it doesn’t allow you to scale beyond an amount of data that can be held in the memory or disk of a single machine. Much like the replicated serving pattern that was previously described, it is simple to build a replicated scatter/gather system. But at a certain data size, it is necessary to introduce sharding in order to build a system that can hold more data than can be stored on a single machine.

> Mặc dù việc áp dụng mẫu scatter/gather với dữ liệu nhân bản cho phép bạn giảm thời gian xử lý cần thiết để phục vụ các yêu cầu người dùng, nó không cho phép bạn mở rộng vượt quá lượng dữ liệu có thể chứa trong bộ nhớ hoặc đĩa của một máy duy nhất. Tương tự như mẫu phục vụ nhân bản đã mô tả trước đó, việc xây dựng một hệ thống scatter/gather nhân bản là đơn giản. Nhưng đến một kích thước dữ liệu nhất định, cần phải đưa vào phân mảnh để xây dựng một hệ thống có thể chứa nhiều dữ liệu hơn mức có thể lưu trên một máy duy nhất.

Previously, when sharding was introduced to scale replicated systems, the sharding was done at a per-request level. Some part of the request was used to determine where the request was sent. That replica then handled all of the processing for the request and the response was handed back to the user. Instead, with scatter/gather sharding, the request is sent to all of the leaf nodes (or shards) in the system. Each leaf node processes the request using the data that it has loaded in its **shard**. This partial response is then returned to the root node that requested data, and that root node merges all of the responses together to form a comprehensive response for the user.

> Trước đây, khi phân mảnh được đưa vào để mở rộng các hệ thống nhân bản, việc phân mảnh được thực hiện ở mức từng yêu cầu. Một phần nào đó của yêu cầu được dùng để xác định yêu cầu được gửi đi đâu. Bản sao đó sau đó xử lý toàn bộ việc xử lý cho yêu cầu và phản hồi được trả lại cho người dùng. Ngược lại, với phân mảnh scatter/gather, yêu cầu được gửi tới tất cả các nút lá (hay các mảnh) trong hệ thống. Mỗi nút lá xử lý yêu cầu bằng dữ liệu đã nạp trong mảnh của nó. Phản hồi thành phần này sau đó được trả về cho nút gốc đã yêu cầu dữ liệu, và nút gốc đó gộp tất cả các phản hồi lại với nhau để tạo thành một phản hồi toàn diện cho người dùng.

As a concrete example of this sort of architecture, consider implementing search across a very large document set (all patents in the world, for example); in such a case, the data is too large to fit in the memory of a single machine, so instead the data is sharded across multiple replicas. For example, patents 0-100,000 might be on the first machine, 100,001-200,000 on the next machine, and so forth. (Note that this is not actually a good sharding scheme since it will continually force us to add new shards as new patents are registered. In practice, we’d likely use the patent number modulo the total number of shards.) When a user submits a request to find a particular word (e.g., “rockets”) in all of the patents in the index, that request is sent to each shard, which searches through it’s patent shard for patents which match the word in the query. Any matches that are found are returned to the root node in response to the shard request. The root node then collates all of these responses together into a single response that contains all the patents that match the particular word. The operation of this search index is illustrated in Figure 7-3 .

> Như một ví dụ cụ thể về kiểu kiến trúc này, hãy xét việc triển khai tìm kiếm trên một tập tài liệu rất lớn (chẳng hạn, tất cả bằng sáng chế trên thế giới); trong trường hợp đó, dữ liệu quá lớn để vừa trong bộ nhớ của một máy duy nhất, nên thay vào đó dữ liệu được phân mảnh trên nhiều bản sao. Ví dụ, các bằng sáng chế 0-100.000 có thể nằm trên máy đầu tiên, 100.001-200.000 trên máy tiếp theo, v.v. (Lưu ý rằng đây thực ra không phải một sơ đồ phân mảnh tốt vì nó sẽ liên tục buộc ta phải thêm mảnh mới khi có bằng sáng chế mới được đăng ký. Trong thực tế, ta có lẽ sẽ dùng số bằng sáng chế chia lấy dư cho tổng số mảnh.) Khi một người dùng gửi yêu cầu tìm một từ cụ thể (ví dụ, "rockets") trong tất cả các bằng sáng chế trong chỉ mục, yêu cầu đó được gửi tới mỗi mảnh, và mỗi mảnh tìm qua mảnh bằng sáng chế của nó để tìm các bằng sáng chế khớp với từ trong truy vấn. Bất kỳ kết quả khớp nào tìm được đều được trả về nút gốc để đáp lại yêu cầu phân mảnh. Sau đó nút gốc đối chiếu tất cả các phản hồi này lại thành một phản hồi duy nhất chứa tất cả các bằng sáng chế khớp với từ cụ thể đó. Hoạt động của chỉ mục tìm kiếm này được minh họa trong Hình 7-3.

## Hands On: Sharded Document Search — Thực hành (Hands On): Tìm kiếm Tài liệu có Phân mảnh

The previous example scattered the different term requests across the cluster, but this only works if all of the documents are present on all of the machines in the scatter/ gather tree. If there is not enough room for all of the documents in all of the leaves in the tree, then sharding must be used to put different sets of documents onto different leaves.

> Ví dụ trước đã phát tán các yêu cầu theo từng từ khác nhau ra khắp cụm, nhưng điều này chỉ hoạt động nếu tất cả các tài liệu đều hiện diện trên tất cả các máy trong cây scatter/gather. Nếu không đủ chỗ cho tất cả các tài liệu trên tất cả các lá trong cây, thì phải dùng phân mảnh để đặt các tập tài liệu khác nhau lên các lá khác nhau.

This means that when a user makes a request for all documents that match the words “cat” and “dog,” the request is actually sent out to every leaf in the scatter/gather system. Each leaf node returns the set of documents that it knows about that matches “cat” and “dog.” Previously, the root node was responsible for performing the intersection of the two sets of documents returned for two different words. In the sharded case, the root node is responsible for generating the union of all of the documents returned by all of the different shards and returning this complete set of documents back up to the user.

> Điều này có nghĩa là khi một người dùng đưa ra yêu cầu tìm tất cả các tài liệu khớp với các từ "cat" và "dog", yêu cầu thực ra được gửi ra mọi lá trong hệ thống scatter/gather. Mỗi nút lá trả về tập các tài liệu mà nó biết và khớp với "cat" và "dog". Trước đây, nút gốc chịu trách nhiệm thực hiện phép giao của hai tập tài liệu được trả về cho hai từ khác nhau. Trong trường hợp phân mảnh, nút gốc chịu trách nhiệm tạo phép hợp của tất cả các tài liệu được trả về bởi tất cả các mảnh khác nhau và trả tập tài liệu hoàn chỉnh này lên cho người dùng.

In Figure 7-3 , the first leaf serves documents 1 through 10 and returns {doc1, doc5} . The second leaf serves documents 11 through 20 and returns {doc15} . The third leaf serves documents 21 through 30 and returns {doc22, doc28} . The root combines all of these responses together into a single response and returns {doc1, doc5, doc15,

> Trong Hình 7-3, lá thứ nhất phục vụ các tài liệu 1 đến 10 và trả về {doc1, doc5}. Lá thứ hai phục vụ các tài liệu 11 đến 20 và trả về {doc15}. Lá thứ ba phục vụ các tài liệu 21 đến 30 và trả về {doc22, doc28}. Gốc kết hợp tất cả các phản hồi này lại thành một phản hồi duy nhất và trả về {doc1, doc5, doc15,

```
doc22, doc28}.
```

![](images/ch07-91_1.png)

Figure 7-3. Conjunctive query executing in a scatter/gather search system

> Hình 7-3. Truy vấn liên kết (conjunctive query) đang được thực thi trong một hệ thống tìm kiếm scatter/gather

## Choosing the Right Number of Leaves — Chọn Số lượng Lá Phù hợp

It might seem that in the scatter/gather pattern, replicating out to a very large number of leaves would always be a good idea. You parallelize your computation and consequently reduce the clock time required to process any particular request. However, increased parallelization comes at a cost, and thus choosing the right number of leaf nodes in the scatter/gather pattern is critical to designing a performant **distributed system**.

> Có vẻ như trong mẫu scatter/gather, nhân bản ra một số lượng lá rất lớn luôn là một ý tưởng tốt. Bạn song song hóa phần tính toán và nhờ đó giảm thời gian đồng hồ cần thiết để xử lý bất kỳ yêu cầu cụ thể nào. Tuy nhiên, việc tăng song song hóa đi kèm cái giá của nó, và do đó việc chọn đúng số lượng nút lá trong mẫu scatter/gather là tối quan trọng để thiết kế một hệ phân tán có hiệu năng cao.

To understand how this can happen, it’s worth considering two things. The first is that processing any particular request has a certain amount of overhead. This is the time spent parsing a request, sending HTTP across the wire, and so forth. In general, the overhead due to system request handling is constant and significantly less than the time spent in user code processing the request. Consequently, this overhead can generally be ignored when assessing the performance of the scatter/gather pattern. However, it is important to understand that the cost of this overhead scales with the number of leaf nodes in the scatter/gather pattern. Thus, even though it is low cost, as parallelization continues, this overhead eventually dominates the compute cost of your business logic. This means that the gains of parallelization are asymptotic.

> Để hiểu điều này có thể xảy ra như thế nào, đáng để xét hai điều. Điều thứ nhất là việc xử lý bất kỳ yêu cầu cụ thể nào cũng có một lượng chi phí phụ (overhead) nhất định. Đó là thời gian dành để phân tích một yêu cầu, gửi HTTP qua đường truyền, v.v. Nhìn chung, chi phí phụ do hệ thống xử lý yêu cầu là hằng số và nhỏ hơn đáng kể so với thời gian dành trong mã người dùng để xử lý yêu cầu. Do đó, chi phí phụ này nói chung có thể được bỏ qua khi đánh giá hiệu năng của mẫu scatter/gather. Tuy nhiên, điều quan trọng cần hiểu là cái giá của chi phí phụ này tỉ lệ với số lượng nút lá trong mẫu scatter/gather. Vì vậy, dù chi phí thấp, khi song song hóa tiếp tục tăng, chi phí phụ này cuối cùng sẽ lấn át chi phí tính toán của logic nghiệp vụ của bạn. Điều này có nghĩa là lợi ích của song song hóa là tiệm cận (asymptotic).

In addition to the fact that adding more leaf nodes may not actually speed up processing, scatter/gather systems also suffer from the “straggler” problem. To understand how this works, it is important to remember that in a scatter/gather system, the root node waits for requests from all of the leaf nodes to return before sending a response back to the end user. Since data from every leaf node is required, the overall time it takes to process a user request is defined by the slowest leaf node that sends a response. To understand the impact of this, imagine that we have a service that has a 99th percentile latency of 2 seconds. This means that on average one request out of every 100 has a latency of 2 seconds, or put another way, there is a 1% chance that a request will take 2 seconds. This may be totally acceptable at first glance: a single user out of 100 has a slow request. However, consider how this actually works in a scatter/ gather system. Since the time of the user request is defined by the slowest response, we need to consider not a single request but all requests scattered out to the various leaf nodes.

> Ngoài việc thêm nhiều nút lá hơn có thể thực ra không giúp tăng tốc việc xử lý, các hệ thống scatter/gather còn gặp phải vấn đề "kẻ tụt hậu" (straggler). Để hiểu điều này hoạt động ra sao, cần nhớ rằng trong một hệ thống scatter/gather, nút gốc chờ các yêu cầu từ tất cả các nút lá trả về trước khi gửi phản hồi lại cho người dùng cuối. Vì cần dữ liệu từ mọi nút lá, tổng thời gian để xử lý một yêu cầu người dùng được quyết định bởi nút lá phản hồi chậm nhất. Để hiểu tác động của điều này, hãy hình dung ta có một dịch vụ có độ trễ phân vị thứ 99 (99th percentile latency) là 2 giây. Điều này có nghĩa là trung bình cứ 100 yêu cầu thì có một yêu cầu có độ trễ 2 giây, hay nói cách khác, có 1% khả năng một yêu cầu sẽ mất 2 giây. Thoạt nhìn điều này có thể hoàn toàn chấp nhận được: một người dùng trong số 100 người có một yêu cầu chậm. Tuy nhiên, hãy xét xem điều này thực sự hoạt động ra sao trong một hệ thống scatter/gather. Vì thời gian của yêu cầu người dùng được quyết định bởi phản hồi chậm nhất, ta cần xét không phải một yêu cầu đơn lẻ mà là tất cả các yêu cầu được phát tán ra tới các nút lá khác nhau.

Let’s see what happens when we scatter out to five leaf nodes. In this situation, there is a 5% chance that one of these five scatter requests has a latency of 2 seconds (0.99 × 0.99 × 0.99 × 0.99 × 0.99 == 0.95). This means that our 99th percentile latency for individual requests becomes a 95th percentile latency for our complete scatter/gather system. And it only gets worse from there: if we scatter out to 100 leaves, then we are more or less guaranteeing that our overall latency for all requests will be 2 seconds.

> Hãy xem điều gì xảy ra khi ta phát tán ra năm nút lá. Trong tình huống này, có 5% khả năng một trong năm yêu cầu phát tán này có độ trễ 2 giây (0,99 × 0,99 × 0,99 × 0,99 × 0,99 == 0,95). Điều này có nghĩa là độ trễ phân vị thứ 99 của ta cho các yêu cầu riêng lẻ trở thành độ trễ phân vị thứ 95 cho toàn bộ hệ thống scatter/gather. Và nó chỉ tệ hơn từ đó: nếu ta phát tán ra 100 lá, thì ta gần như chắc chắn rằng độ trễ tổng thể cho tất cả các yêu cầu sẽ là 2 giây.

Together, these complications of scatter/gather systems lead us to some conclusions:

> Cùng nhau, những phức tạp này của các hệ thống scatter/gather dẫn ta đến một số kết luận:

- Increased parallelism doesn’t always speed things up because of overhead on each node.
- Increased parallelism doesn’t always speed things up because of the straggler problem.
- The performance of the 99th percentile is more important than in other systems because each user request actually becomes numerous requests to the service.

> - Tăng tính song song không phải lúc nào cũng tăng tốc mọi thứ vì chi phí phụ trên mỗi nút.
> - Tăng tính song song không phải lúc nào cũng tăng tốc mọi thứ vì vấn đề kẻ tụt hậu.
> - Hiệu năng ở phân vị thứ 99 quan trọng hơn so với ở các hệ thống khác vì mỗi yêu cầu người dùng thực ra trở thành nhiều yêu cầu tới dịch vụ.

The same straggler problem applies to **availability**. If you issue a request to 100 leaf nodes, and the probability that any leaf node failing is 1 in 100, you are again practically guaranteed to fail every single user request.

> Cùng vấn đề kẻ tụt hậu đó cũng áp dụng cho tính sẵn sàng. Nếu bạn phát ra một yêu cầu tới 100 nút lá, và xác suất bất kỳ nút lá nào hỏng là 1 trên 100, thì bạn lại gần như chắc chắn sẽ làm hỏng mọi yêu cầu người dùng.

## Scaling Scatter/Gather for Reliability and Scale — Mở rộng Scatter/Gather để Đạt Độ tin cậy và Quy mô

Of course, just as with a sharded system, having a single replica of a sharded scatter/ gather system is likely not the desirable design choice. A single replica means that if it fails, all scatter/gather requests will fail for the duration that the shard is unavailable because all requests are required to be processed by all leaf nodes in the scatter/gather pattern. Likewise, upgrades will take out a percentage of your shards, so an upgrade while under user-facing load is no longer possible. Finally, the computational scale of your system will be limited by the load that any single node is capable of achieving. Ultimately, this limits your scale, and as we have seen in previous sections, you cannot simply increase the number of shards in order to improve the computational power of a scatter/gather pattern.

> Tất nhiên, cũng như với một hệ thống phân mảnh, việc chỉ có một bản sao duy nhất của một hệ thống scatter/gather phân mảnh có lẽ không phải lựa chọn thiết kế mong muốn. Một bản sao duy nhất có nghĩa là nếu nó hỏng, tất cả các yêu cầu scatter/gather sẽ thất bại trong suốt thời gian mảnh đó không khả dụng, vì mọi yêu cầu đều buộc phải được xử lý bởi tất cả các nút lá trong mẫu scatter/gather. Tương tự, các lần nâng cấp sẽ làm mất một phần trăm số mảnh của bạn, nên việc nâng cấp khi đang chịu tải hướng tới người dùng không còn khả thi. Cuối cùng, quy mô tính toán của hệ thống sẽ bị giới hạn bởi mức tải mà bất kỳ nút đơn lẻ nào có khả năng đạt được. Rốt cuộc, điều này giới hạn quy mô của bạn, và như ta đã thấy trong các phần trước, bạn không thể chỉ đơn giản tăng số mảnh để cải thiện sức mạnh tính toán của một mẫu scatter/gather.

Given these challenges of **reliability** and scale, the correct approach is to replicate each of the individual shards so that instead of a single instance at each leaf node, there is a **replicated service** that implements each leaf shard. This replicated, sharded scatter/gather pattern is shown in Figure 7-4 .

> Trước những thách thức về độ tin cậy và quy mô này, cách tiếp cận đúng đắn là nhân bản từng mảnh riêng lẻ sao cho thay vì một thực thể duy nhất tại mỗi nút lá, có một dịch vụ nhân bản hiện thực mỗi mảnh lá. Mẫu scatter/gather nhân bản, phân mảnh này được thể hiện trong Hình 7-4.

![](images/ch07-94_1.png)

Figure 7-4. A sharded, replicated scatter/gatther system

> Hình 7-4. Một hệ thống scatter/gather phân mảnh, nhân bản

Built this way, each leaf request from the root is actually load balanced across all healthy replicas of the shard. This means that if there are any failures, they won’t result in a user-visible outage for your system. Likewise, you can safely perform an upgrade under load, since each replicated shard can be upgraded one replica at a time. Indeed, you can perform the upgrade across multiple shards simultaneously, depending on how quickly you want to perform the upgrade.

> Được xây dựng theo cách này, mỗi yêu cầu lá từ gốc thực ra được cân bằng tải trên tất cả các bản sao khỏe mạnh của mảnh. Điều này có nghĩa là nếu có bất kỳ sự cố nào, chúng sẽ không gây ra một sự gián đoạn mà người dùng nhìn thấy cho hệ thống của bạn. Tương tự, bạn có thể an toàn thực hiện một lần nâng cấp khi đang chịu tải, vì mỗi mảnh nhân bản có thể được nâng cấp từng bản sao một. Thật vậy, bạn có thể thực hiện nâng cấp trên nhiều mảnh đồng thời, tùy thuộc vào mức độ nhanh chóng mà bạn muốn thực hiện nâng cấp.
